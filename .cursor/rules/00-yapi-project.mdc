---
description: YApi 项目身份与架构边界，防止回退到已删除技术栈
alwaysApply: true
---

# YApi 项目核心（始终遵守）

## 架构（不可偏移）

- **前端唯一入口**：`vendors/web/`（Next.js 15 App Router + shadcn/ui + Tailwind）。
- **后端**：`vendors/server/`（Hono + Node），配置在仓库根目录 `config.json`。
- **已废弃、禁止恢复**：`vendors/client/`、Vite、`static/prd` 构建、Ant Design / Redux 作为正式 UI、Koa 作为当前入口。

## 工作目录

- 安装、构建、测试均在 `vendors/` 执行：`npm run dev` | `build` | `test:server`。
- 改前端前先读 `vendors/web/src/lib/api/` 是否已有封装，避免重复造 fetch。

## 文档优先级

1. 根目录 `AGENTS.md`（AI 协作）
2. 根目录 `README.md`（人类上手）
3. 本目录 `.cursor/rules/*.mdc`

## 禁止行为

- 不要未经用户要求新增总结类 markdown（README/AGENTS/规则除外）。
- 不要批量删除 `console.log` / `console.error`。
- 不要估算「需要几天」；用涉及模块与风险描述范围。
- 不要在 Next 中 `require` `vendors/exts/**/client.js`（旧 React 插件 UI 路径已失效）。

## Git 分支（Cloud Agent）

- 功能分支：`cursor/<描述>-5bb5`（全小写），基于 `main`。
- 改 `server`/`common` 后跑 `cd vendors && npm run test:server`；改 `web` 后跑 `npm run build`。
