---
description: YApi 项目身份与架构边界
alwaysApply: true
---

# YApi 项目核心

## 架构

- **后端**：`server/` 自包含（config、controllers、services、utils、lib、依赖、测试均在 server 内）
- **前端**：`client/`
- **根目录**：workspaces 编排 + `scripts/`，禁止把后端配置/依赖散落在根目录

## 命令（仓库根目录）

`npm run dev` | `dev-server` | `dev-client` | `test:server` | `build`

## 配置

复制 `server/config.example.json` → `server/config.json`，配置 **PostgreSQL**（见 `server/.env.example`）。**全新开发**，仅需空库 + `npm run install-server`，不做 Mongo/历史数据迁移。Docker 见 `deploy/`。
