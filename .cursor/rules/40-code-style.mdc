---
description: 代码风格、注释、改动范围与用户沟通偏好
alwaysApply: true
---

# 代码风格与协作

## 改动原则

- **最小范围**：只改完成任务所需的文件，不顺手重构无关代码。
- **遵循现有模式**：命名、目录、import 方式与相邻文件一致。
- **避免过度工程**：不为一两行逻辑抽公共函数；不堆砌不可能触发的 fallback。

## 注释

- 非显而易见逻辑用**中文**注释说明业务意图或技术原因。
- 新文件顶部用中文块注释说明模块职责（参考现有 `page.tsx`、`lib/api/*.ts`）。

## Console

- **禁止**删除既有 `console.log` / `console.error`（除非用户明确要求清理）。

## 测试

- 仅在有真实行为价值时加测试；不写只断言常量的无意义测试。
- 服务端改动后运行 `npm run test:server`（仓库根目录）。

## 与用户沟通（简体中文）

- 回复使用简体中文，技术准确、句子完整。
- 引用已有代码用 ```startLine:endLine:filepath 格式。
- 不要主动写用户未要求的额外 md 总结文件。
