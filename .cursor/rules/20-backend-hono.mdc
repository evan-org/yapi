---
description: 后端 Hono 分层与配置约定
globs:
  - server/**/*
---

# 后端（server/）

- 入口：`server/app.ts`（**ESM** + `import` 语法；开发/生产 `tsx` 或 `node dist/app.js`）
- 源码为 TypeScript（`*.ts`），`package.json` 含 `"type": "module"`，编译输出 `server/dist/`
- 配置：`server/config/load-config.ts` 仅读 **环境变量** + `server/.env`（见 `.env.example`）；运行时 `server/runtime.ts`
- `WEBROOT` = `server/` 目录（`runtime.ts` 自动识别 `dist/` 编译目录）

## 分层（必须遵守）

```
handlers/ → routes/modules/*.routes.ts → controllers/*.ts → services/*.ts → repositories/*.repo.ts → db/
```

- **handlers/**：`http.ts` 装配 Hono（中间件、路由挂载），`app.ts` 只做进程入口
- **routes/**：`index.ts` 入口；`modules/` 各模块路由；`lib/action-runner.ts` 执行 Action
- **controllers/**：HTTP 薄层，只做 ctx 适配；**必须**经 `services/` 访问数据，禁止直连 `repositories/`；鉴权委托 `services/auth.service.ts`
- **shared/config.ts**：配置读取统一出口，新代码避免直接 `yapi.WEBCONFIG`
- **services/**：业务逻辑，不依赖 Hono Context；返回 `ServiceResult` + `replyServiceResult`
- **repositories/**：`*.repo.ts` 表级 SQL
- **shared/**：响应契约等横切出口（`shared/response.ts`）
- **lib/**：`context.ts`（AppContext）、`bind-routes.ts`（RouteBinder）
- Mock：`middleware/mock.ts` + `mock-handler.ts`
- 新接口：在 `routes/modules/` 加路由 → `services/` 写逻辑 → `controllers/` 薄封装
- 测试：`npm run test:server`（在仓库根目录）

## 配置约定

- 复杂结构使用 `YAPI_*` JSON 环境变量（如 `YAPI_LDAP_LOGIN`、`YAPI_INTEGRATIONS`）
