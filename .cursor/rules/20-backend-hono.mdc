---
description: Hono 后端与 common 共享库
globs: server/**/*,common/**/*
---

# 后端（server + common）

## 入口与配置

- 入口：`server/app.js`（Hono）；页面由 Next 承载。
- `server/yapi.js`：`WEBROOT` = 仓库根目录（与 `client/`、`exts/` 同级）；`config.json` 在根目录。
- 新路由：`server/router.js` + `server/controllers/`。

## 插件

- 服务端插件：`exts/` + `server/plugin.js`。

## 测试

- 修改后：`npm run test:server`（根目录）。

## Mock

- Mock 中间件在 `server/middleware/`；共享变量见 `common/variable.js`。
