---
description: 插件与构建脚本
globs: exts/**/*,scripts/**/*,client/src/lib/plugins/**/*
---

# 插件与脚本

## 插件目录 `exts/`

- 业务能力在 `server.js`；`import_data` 等钩子在此注册。
- 勿在 Next 中加载 `client.js`。

## 构建

- `scripts/generate-plugin-module.js` → `client/src/lib/plugins/registry.ts`
- `scripts/build-next-client.js` → 产物 `client/.next/`
- Next 反代：`client/next.config.ts` 中 `/api` → `YAPI_API_URL`
